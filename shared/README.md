# Shared

This folder is reserved for code that must be consumed by both the frontend and backend.

Examples:
- shared constants
- validation schemas
- portable DTO/type definitions

Frontend-only mock data now lives in `frontend/lib/mock-data`.
